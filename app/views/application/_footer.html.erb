<% if @cms_page %>
  <%= cms_fragment_content(:footer).present? ? cms_fragment_render(:footer) : render("newsletter_form") %>
<% else %>
  <%= content_for?(:footer) ? content_for(:footer) : render("newsletter_form") %>
<% end %>

<%= render "navigation" %>
